* text=auto eol=lf

# The eol=lf baseline above suffices as-is. Add binary paths and
# linguist-vendored/generated/diff-driver rules here only if your stack needs them.

# Retained producer bytes are immutable evidence. Do not normalize their source whitespace.
docs/specs/empanel-replacement/evidence/** -whitespace
