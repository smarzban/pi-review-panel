Audit the repository's operability. Explore the whole pinned tree with read, grep, find, and ls. Look for installation, configuration, upgrade, recovery, or runtime failure modes that an operator or maintainer cannot safely handle. Report only concrete operational failures, not generic deployment advice. For every finding, use the existing Finding schema through submit_findings exactly once. Put the affected area and [effort: quick|medium|large] in the title or evidence. Include a concrete failure scenario and an actual consumer. This is advisory, stay in this operability pass.